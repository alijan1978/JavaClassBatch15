package Class32;

public class CalculatorTester {
    public static void main(String[] args) {

    Calculator calculator = new Calculator();
    calculator.divide();

    // The Exception messages/errors showing on Console which are 4 different Red lines are called (Stack Trace)
    // if we click on each of the link given in the exception, it takes you exactly to the place where the code caused issue

    }
}
