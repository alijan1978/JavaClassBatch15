package Class16;

public class StudentTester {
    public static void main(String[] args) {
        Student student=new Student();
        student.name="Khajoo";
        student.id="A4005";
        student.schoolName="EVHS";
        student.weight=70.5;

        Student student1=new Student();
        // for every single Object of Instance variable we reserve a space in memory
        // but if we use Static variable, it only save only one memory space
        // the good thing about Static is, that if we create hundred objects different info in that class, they can access Static




    }
}
