package Class16;

public class Student {
    // With instance variable, for every object name we can have different values
    //But downside is that it reserve space in memory for every object we create.
    // If we use Static variable, the object name of static class will only reserve one memory location.

    String name;
    String id;
    String schoolName;
    int age;
    double weight;



}
