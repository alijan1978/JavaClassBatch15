package Class3;

public class IfElseStatement2 {
    public static void main(String[] args) {

        int age = 17;
        System.out.println(age < 18);

        if (age <18 ){
            System.out.println(" You are not eligible to work because you are under age");
        }
        boolean inSyntaxBootCamp = true;

        /* If conditions works based on boolean true or false, so if we have a true inside the if () it executes
        all the codes inside {} , but if it is false doesn't execute any code inside {}
         */

        if(true){
            System.out.println("Time to start practicing Java ");
        }

// speed 2x, time 2:27

    }
}
